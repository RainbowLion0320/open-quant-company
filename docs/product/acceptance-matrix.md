# Open Quant Company — PRD/Spec 验收矩阵

> 日期: 2026-06-02 | 来源: 文档治理基线
> 用途: 追踪 7 个能力域从 PRD/spec → 代码 → 测试 → API/Web → 手工验收的完整链路
> 维护规则: 本文件记录当前可验收能力链路，不作为 sprint 日志；历史计划和任务过程通过 git 追溯。

## 1. 数据管道 (Data Pipeline)

| # | PRD/Spec 条目 | 代码文件 | 测试 | API / Web | 手工验收 | 状态 | 缺口 |
|---|--------------|---------|------|-----------|---------|------|------|
| 1.1 | DataRegistry 声明式维度配置 | `config/settings.yaml` → `data/storage/dimensions.py` | `test_datahub_contracts.py:test_data_registry_contract_is_valid` | `GET /api/system/db-health` → `/datahub?tab=health` | registry validate 无错误，扫描使用注册表路径 | OK | — |
| 1.2 | DataHub 统一路径 `dimension_path()` + 原子写入 | `data/storage/datahub.py` | `test_datahub_contracts.py:test_datahub_expands_registry_dimension_paths` | — | `python -c "from data.storage.datahub import DataHub; print(DataHub().audit())"` | OK | — |
| 1.3 | Manifest 元数据记录 (schema/row_count/hash) | `data/storage/datahub.py` | `test_datahub_contracts.py:test_datahub_writes_manifest_for_parquet` | `GET /api/system/db-health` (DB Health 页面) | 检查 `var/store/_manifest/datasets.parquet` 有记录 | OK | — |
| 1.4 | fcntl 锁 + 去重追加 | `data/storage/datahub.py:append_parquet()` | `test_boundary.py` (DataHub追加去重写入) | — | 并发写入测试 | OK | — |
| 1.5 | AKShare 3 源 fallback (新浪→东财→腾讯) | `data/ingestion/fetcher.py` | `test_data_fetcher_resilience.py` | — | `python -c "from data.ingestion.fetcher import get_stock_daily; print(get_stock_daily('000001'))"` | OK | — |
| 1.6 | API 安全阀 (默认不触网) | `data/ingestion/fetcher.py` | `test_architecture_contracts.py:test_stock_daily_read_path_does_not_implicitly_fetch_api` | — | 设置 `QUANT_ALLOW_API_FALLBACK=0` 验证不触网 | OK | — |
| 1.7 | 请求节流 3s + 指数退避重试 3 次 | `data/ingestion/fetcher.py` | `test_data_fetcher_resilience.py` | — | 观察 cron 日志无频率限制错误 | OK | — |
| 1.8 | 6 规则数据清洗 | `data/quality/cleaner.py` | `test_data_cleaner_contracts.py` | — | `python -c "from data.cleaner import DataCleaner; print(DataCleaner().rule_count)"` | OK | — |
| 1.9 | 财务数据三层缓存 (内存→Parquet→API) | `data/market/financials.py` | `test_datahub_contracts.py:test_financials_uses_canonical_symbol_path` | — | 第二次查询同一股票财务数据应瞬间返回 | OK | — |
| 1.10 | PIT 特征构建 (as-of 日期视图) | `data/features/feature_store.py`, `scripts/build_features.py` | `test_architecture_contracts.py:test_build_features_import_is_safe`, `test_asof_pit_feature_view.py` | — | `python scripts/build_features.py --frequency daily` 生成 `var/store/features/YYYY-MM-DD.parquet` | OK | — |
| 1.11 | Cron Logger (JSONL + 自动轮转 500 行) | `data/ops/cron_logger.py` | `test_cron_logger_contracts.py` | `GET /api/system/cron-jobs` → `ActivityMonitor.vue` cron 状态 | 检查 `var/store/_cron_log/` 有日志文件 | OK | — |
| 1.12 | DataRegistry.validate() 合约检验 | `data/storage/dimensions.py` | `test_datahub_contracts.py:test_data_registry_contract_is_valid` | `DatabaseHealth.vue` 健康状态列 | 启动时自动运行 validate() | OK | — |
| 1.13 | DB Health 扫描 | `data/storage/results_db.py` | `test_datahub_contracts.py:test_db_health_scans_moneyflow_symbol_and_tushare_daily` | `GET /api/system/db-health` → `DatabaseHealth.vue` | Web 页面按 DataRegistry 启用维度展示状态表格 | OK | — |

## 2. 信号系统 (Signal System)

| # | PRD/Spec 条目 | 代码文件 | 测试 | API / Web | 手工验收 | 状态 | 缺口 |
|---|--------------|---------|------|-----------|---------|------|------|
| 2.1 | 巴菲特三重过滤 (能力圈→护城河→安全边际) | `signals/buffett.py` | `test_buffett_financial_sector.py` | `GET /api/strategies/buffett` → `Strategies.vue` 信号展开 | `make scan` 生成 `var/store/signals/buffett_scan.parquet` | OK | — |
| 2.2 | 多因子五维加权打分 | `signals/multifactor.py` | `test_boundary.py` (动量计算), `test_sector_pipeline.py:test_multifactor_weights_sum_to_one` | `GET /api/strategies/multifactor` → `Strategies.vue` 信号展开 | 固定日期+股票池评分结果可复现，detail 含 industry 分 | OK | — |
| 2.3 | ML LightGBM 信号 | `signals/ml_signals.py`, `models/lgbm_runtime.py` | `test_architecture_contracts.py:test_model_evaluate_datetime_index_icir_does_not_crash`, `test_ml_lgbm_closure.py` | `GET /api/strategies/ml_lgbm` | `python scripts/tune_model.py` 和 `weekly_retrain.py` | OK | — |
| 2.4 | 控制论自适应 Hybrid Regime 检测 + 参数调整 | `cybernetics/orchestrator.py`, `cybernetics/hmm.py`, `cybernetics/regime.py`, `cybernetics/regime_policy.py`, `cybernetics/regime_scoring.py`, `cybernetics/regime_state.py`, `data/reference/models/regime_hmm/` | `test_market_regime_v2.py`, `test_hmm_engine.py`, `test_regime_scoring.py`, `test_regime_state.py` | `GET /api/market/regime` → `Market.vue`, `GET /api/pipeline/market-regime` → `Pipeline.vue` | Hybrid 默认引擎；规则评分输出 score/components，HMM 输出概率/confidence/entropy；不一致低置信 blended vote；实时 confirmed regime 按唯一观测日执行 `min_dwell=3` | OK | — |
| 2.5 | 因子 DSL 表达式引擎 (声明式因子) | `signals/expression.py` | `test_boundary.py` (Ret/MA/Std/Delta/缺失输入) | — | `python -c "from signals.expression import Ref,MA,Std,Delta,Ret; ..."` | OK | — |
| 2.6 | DSL 公式解析 (LLM→计算) | `signals/dsl_parser.py` | `test_boundary.py` (公式解析) | — | `python -m research.factors.hypothesis.cli` | OK | — |
| 2.7 | 横截面排名 → 受限 buy list + hold rows | `signals/selection.py`, `signals/tradability.py` | `test_boundary.py` (apply_ranked_buys), `test_ml_lgbm_closure.py` | — | 验证信号文件符合 schema (symbol, score, signal, detail.selection_*)，ST/退市风险行保留 hold 且带 `detail.tradability_blocked` | OK | — |
| 2.8 | 策略插件注册表 (动态 import) | `data/strategy/plugins.py` | `test_architecture_contracts.py:test_enabled_strategy_plugins_have_runners` | `GET /api/strategies` → `Strategies.vue` | 新增策略只需改 yaml 配置 | OK | — |
| 2.9 | Regime 自适应权重调整 | `signals/multifactor.py` | `test_sector_pipeline.py:test_regime_affects_market_score_but_not_industry` | — | bull/bear/sideways 三种 regime 下权重分布不同 | OK | — |
| 2.10 | 行业动量因子集成 | `signals/multifactor.py:_industry_score()` | — | 多因子评分含 industry 维度, 组合敞口 API | 行业动量已纳入五维评分, detail 含 industry 分 | OK | — |
| 2.11 | 策略研究治理和晋级门槛 | `research/strategy_governance.py` | `test_strategy_research_governance.py` | `GET /api/strategies/governance` → `Strategies.vue` | 内置策略分层、paper/production 门槛、ML 默认为 paper | OK | — |
| 2.12 | 因子研究诊断 | `signals/factor_research.py` | `test_strategy_research_governance.py:test_factor_diagnostics_rank_ic_quantile_spread_and_correlation_clusters` | — | 输出 IC/ICIR/分组收益 spread/相关性聚类 | OK | — |
| 2.13 | Market Regime 离线训练与晋级 | `research/regime/`, `scripts/train_market_regime.py` | `test_regime_training.py` | `reports/regime_training/summary.json` | champion/challenger、walk-forward、策略 A/B、默认不自动替换生产公式 | OK | — |
| 2.14 | Market Regime 挣钱导向训练 | `research/regime/`, `scripts/train_market_regime_profit.py`, `cybernetics/regime_policy.py` | `test_regime_profit_training.py` | `reports/regime_profit_training/summary.json` | 可交易资产 risk-on/risk-off、强 baseline、walk-forward OOS、champion 同标准诊断、best validated 选择，`w0611` 已作为规则评分基线并由统一 production policy 常量约束 | OK | — |
| 2.15 | Strategy Catalog 策略元数据权威目录 | `research/strategy_catalog.py`, `data/strategy/catalog.py` | `test_strategy_catalog.py` | `GET /api/strategies/catalog` → `Strategies.vue` | 策略目录含类型、层级、生命周期、config_key、数据需求和输出契约 | OK | — |
| 2.16 | 策略运行范围生产隔离 | `data/strategy/plugins.py`, `scripts/compute_signals.py`, `web/api/jobs.py` | `test_strategy_runtime_gates.py` | `POST /api/strategies/run` (`mode=production/research`) | production 默认排除 candidate；Strategy Lab 候选按钮显式 research scan | OK | — |
| 2.17 | 首批候选策略池 | `signals/candidates/*.py`, `config/settings.yaml` | `test_candidate_strategy_contracts.py`, `test_strategy_competition.py` | `GET /api/strategies/catalog`, `astroq strategy compete --run-backtest --json` | 8 个候选策略输出统一 StrategySignalRows，默认候选买入上限 ≤20；12 策略统一竞赛已生成 OOS/IC/ICIR 证据，候选策略按门槛保持 candidate/paper/production recommendation | OK | — |

## 3. 回测引擎 (Backtest Engine)

| # | PRD/Spec 条目 | 代码文件 | 测试 | API / Web | 手工验收 | 状态 | 缺口 |
|---|--------------|---------|------|-----------|---------|------|------|
| 3.1 | N 策略锦标赛对比 | `backtest/run_all_strategies.py` | — | `GET /api/backtest` → `/strategy-lab?tab=backtest` (`Backtest.vue`) | `make backtest` 输出排名表 | OK | — |
| 3.2 | 15 项风险指标 (Sharpe/Sortino/Calmar...) | `backtest/analytics.py`, `data/rates/risk_free_rates.py` | `test_boundary.py` (Sharpe/MaxDD/WinRate/Beta/Alpha), `test_risk_free_rates.py` | `Backtest.vue` 雷达图 | 手工验证 Sortino 用 RMS 法、Beta 用 cov 矩阵；Sharpe/Alpha 必须使用本地 risk-free curve，缺数据停止计算 | OK | — |
| 3.3 | PIT 零前视偏差 | `backtest/run_all_strategies.py` | `test_backtest_pit_contracts.py` | — | 构造未来暴涨样本 → 策略不提前买入 | OK | — |
| 3.4 | Regime 回测用生产 policy 且滞后一月 | `backtest/run_all_strategies.py:build_production_regime_map()` | `test_architecture_contracts.py:test_backtest_regime_replay_uses_production_policy_not_monthly_ma_chain` | — | 历史回放 `CHAMPION_POLICY`，当月只使用上一期可得 regime，禁止旧 MA 链路 | OK | — |
| 3.5 | 巴菲特滚动窗口逐年评分 | `backtest/buffett_real_scorer.py` | — | — | 验证滚动评分器每次策略评分前重置年度缓存，且只使用当期可得财报 | OK | — |
| 3.6 | 回测结果可复现 | `backtest/run_all_strategies.py` / deterministic fixture | `test_backtest_reproducibility.py` | — | 同一数据+种子 → 同一结果 | OK | — |
| 3.7 | 生产共享回测流水线 | `backtest/pipeline_runner.py` + `pipeline/*` | `test_backtest_pipeline_runner_contracts.py` | — | `PipelineBacktest` 按 Alpha→Portfolio→Risk→Execution 生产阶段运行，并过滤缺失价格列 | OK | — |
| 3.8 | 基准使用上证综指 (非个股) | `config/settings.yaml` backtest.benchmark | — | — | 确认 benchmark=sh000001 (非 000001) | OK (已修复) | — |
| 3.9 | 约束组合构建 | `pipeline/portfolio.py:ConstrainedPortfolioConstructor` | `test_strategy_research_governance.py:test_constrained_portfolio_constructor_caps_sector_and_single_name_weight` | — | Top-N 同时受单票/行业/总仓位上限约束 | OK | — |
| 3.10 | 候选策略证据报告契约 | `research/strategy_evaluation.py`, `backtest/run_all_strategies.py` | `test_strategy_backtest_evidence.py`, `test_strategy_evaluation.py` | `var/store/research/strategy_evidence/*.json` | 报告含强基准、metrics、OOS、成本、regime breakdown、alpha evidence、data readiness 和 promotion decision；每个策略写入 benchmark、fixed-weight、timing、breadth、peer 和 champion measured baselines；缺证据明确 blocked | OK | — |

## 4. 执行层 (Execution Layer)

| # | PRD/Spec 条目 | 代码文件 | 测试 | API / Web | 手工验收 | 状态 | 缺口 |
|---|--------------|---------|------|-----------|---------|------|------|
| 4.1 | PaperBroker 模拟券商 (ABC 接口) | `broker/base.py`, `broker/paper_core.py`, `broker/paper_orders.py` | `test_boundary.py` (买入/卖出/T+1/资金不足) | `/portfolio` + `GET /api/portfolio/*` | 手工下单 → 验证持仓/余额/NAV 更新 | OK | — |
| 4.2 | T+1 交易约束 | `broker/paper_orders.py` | `test_boundary.py` (T+1限制卖出/隔日解除) | — | 当日买入的股票当日不能卖出 | OK | — |
| 4.3 | 5 规则风控 | `broker/risk.py` | `test_broker_risk_persistence_allocator.py:test_risk_manager_rejects_each_configured_limit`, `test_broker_risk_persistence_allocator.py:test_risk_manager_enforces_daily_order_count` | — | 逐规则验证 (仓位上限/总敞口/日频/熔断/单笔) | OK | — |
| 4.4 | 熔断机制 (回撤 > 阈值自动拒绝买单) | `broker/risk.py` | `test_broker_risk_persistence_allocator.py:test_risk_manager_rejects_each_configured_limit` | — | 模拟回撤超限 → 验证买单全拒绝 | OK | — |
| 4.5 | Parquet 状态持久化 (trades/nav/state) | `broker/persistence.py`, `broker/state.py` | `test_broker_risk_persistence_allocator.py:test_paper_state_persistence_round_trip_uses_public_state_model` | — | 创建 Broker → 下单 → 重启 → 状态恢复一致 | OK | — |
| 4.6 | Cron 调度 (15:30 扫描 + 09:30 执行) | `scripts/compute_signals.py`, `scripts/execute_paper_trades.py` | — | `POST /api/strategies/run` (手动触发策略扫描) | GitHub Actions `daily-scan.yml` 正常执行 | OK | — |
| 4.7 | Cron 日志可观测 | `data/ops/cron_logger.py` | — | `GET /api/system/cron-jobs` + JSONL 文件 | 每次 cron 执行后日志 JSONL 有记录 | OK | — |
| 4.8 | 多资产交易所 (差异化费率) | `broker/exchange.py` | `test_broker_risk_persistence_allocator.py:test_exchange_costs_are_asset_specific` | — | A股 vs ETF 费率不同 | OK | — |
| 4.9 | Agent paper order preview/proposal/submit/cancel gate | `broker/paper_core.py`, `agent_os/runtime.py`, `broker/persistence.py`, `web/frontend/src/views/CEOOffice.vue` | `test_agent_os_contracts.py`, `test_data_layout_contracts.py`, `test_ceo_office_frontend_contracts.py` | `POST /api/agent/paper/proposals`, `POST /api/agent/paper/actions/{id}/submit`, `POST /api/agent/paper/actions/{id}/cancel`, `astroq agent paper propose/submit/cancel`, `/` CEO Office message-inline action card | PaperBroker preview 不改现金/持仓/订单；通过 preview 后只创建 `approval_required` 的 `paper_order` action 和 evidence artifact；approved submit 会重新 preview/risk gate，过期或 stale preview 会 blocked，成功后写 `AgentRun`、reconciliation evidence、PaperBroker state/trades/NAV；queued paper action 可通过专用 cancel gate 取消并写 reconciliation evidence；已提交但仍 active 的 PaperBroker order 只有 broker 确认可撤时才写 `order_canceled` 对账，已成交或缺失订单返回 blocked 证据；Action detail API 返回 paper reconciliation summaries；CEO Office 在消息内 action card 显示 paper preview/risk 摘要、审批/提交/取消入口，并可从 run history 打开 reconciliation evidence | OK | — |

## 5. Web 平台 (Web Platform)

| # | PRD/Spec 条目 | 代码文件 | 测试 | API / Web | 手工验收 | 状态 | 缺口 |
|---|--------------|---------|------|-----------|---------|------|------|
| 5.1 | Vue 3 SPA + Pinia + ECharts + Tailwind | `web/frontend/` | — | 8 个一级入口 + 二级 tab 工作区 | `npm run build` 通过 | OK | — |
| 5.2 | FastAPI 14 业务路由模块 | `web/api/routes/` (14 文件) | `test_web_system_contracts.py` (strategy jobs 路由), `test_agent_os_contracts.py` | 全部业务路由模块 | `python -m uvicorn web.api.app:create_app --factory` 启动无报错 | OK | — |
| 5.3 | WebSocket 实时进度推送 | `web/api/ws.py`, `web/api/jobs.py` | `test_websocket_contracts.py` | Strategy run 进度条 | 触发策略扫描 → 前端进度条实时更新 | OK | — |
| 5.4 | DuckDB :memory: 零锁查询 | `web/api/db.py` / `data/storage/db.py` | `test_boundary.py` (DuckDB CRUD) | 所有数据查询端点 | Web 页面数据加载无延迟 | OK | — |
| 5.5 | API 错误响应统一 + 稳定端点 response_model | 各路由文件 + `web/api/errors.py` + `web/api/schemas/*` | `test_web_system_contracts.py`, `test_market_route_contracts.py`, `test_pipeline_route_contracts.py` | — | 4xx/5xx 错误结构一致，关键成功响应有 Pydantic schema | OK | — |
| 5.6 | Settings API YAML 读写 + schema 配置中心 + 审计 | `web/api/routes/settings.py`, `web/api/config_schema/`, `ConfigCenter.vue` | `test_audit.py`, `test_auth.py`, `test_settings_schema_contracts.py` | `/system?tab=settings`, `/system?tab=config`, `GET /api/settings/schema` | 基础设置由 Settings 管理；Config Center schema 返回 groups + sections；dotted section 写回嵌套 YAML；保存写入 audit ledger | OK | — |
| 5.7 | 市场总览 (regime + multi_asset + macro + hot sectors) | `web/api/routes/market.py` + `web/api/routes/sectors.py` | `test_market_route_contracts.py`, `test_web_system_contracts.py` | `Market.vue` | Regime orb + 纯数字 score + 4 个核心小仪表盘 + Confirmed/Raw/Pending/Dwell 状态卡 + 核心指数相对强弱图 + 宏观快照 + Top5 热门行业脉冲；页脚只保留 REGIME/FRESH、CEO 会话模型上下文和三态纯色系统健康灯，策略明细归属策略实验室 | OK | — |
| 5.8 | DB Health 注册表维度监控 | `web/api/routes/system.py` | `test_web_system_contracts.py:test_db_health_scans_new_registry_dimensions` | `/datahub?tab=health` → `DatabaseHealth.vue` | DataRegistry 维度状态表格 + 修复操作 | OK | — |
| 5.9 | CodeGraph 代码图谱 + 架构诊断 | `web/api/routes/codegraph.py`, `web/api/services/codegraph.py`, `web/api/services/codegraph_diagnostics.py` | `test_codegraph_contracts.py` | `/system?tab=codegraph` → `CodeGraph.vue` | 模块/文件/符号图谱可下钻，索引状态和显式 Sync/Rebuild 可用；架构诊断用确定性规则展示循环依赖、跨层调用、热点、孤岛候选等风险 | OK | — |
| 5.10 | Test Design Intelligence | `web/api/routes/system.py`, `web/api/services/system_tests.py`, `astrolabe_cli/design_intelligence.py`, `astrolabe_cli/commands/test_system` | `test_test_design_intelligence.py`, `test_web_system_contracts.py`, `test_cli_ops_commands.py`, `test_frontend_i18n_contracts.py` | `/system?tab=tests` → `TestDesign.vue` | `astroq test design --json` 写入 `var/artifacts/tests/design/latest.json`；Web 只读展示测试设计分数、风险矩阵、测试图谱、用例审查器和异味诊断 | OK | — |
| 5.11 | AST Intelligence 重复实现检测 | `astrolabe_cli/ast_intelligence.py`, `scripts/ast_frontend_collector.mjs`, `web/api/services/system_ast.py` | `test_ast_intelligence.py`, `test_web_system_contracts.py` | `/system?tab=ast` → `AstIntelligence.vue` | `astroq architecture ast --json` 写入 `var/artifacts/architecture/ast/latest.json`；Web 只读展示重复风险分、clone groups、模块热力和诊断 inspector | OK | — |
| 5.12 | Lifecycle Readiness 全周期门禁 | `astrolabe_cli/commands/lifecycle.py`, `web/api/services/system_lifecycle.py`, `LifecycleReadiness.vue` | `test_lifecycle_readiness.py`, `test_strategy_competition.py`, `test_strategy_backtest_evidence.py` | `/system?tab=lifecycle` → `LifecycleReadiness.vue` | `astroq lifecycle check --json` 写入 `var/artifacts/lifecycle/latest.json`；Web 只读展示 source capability、data freshness、strategy evidence、execution readiness 和 blockers | OK | — |
| 5.13 | 前端构建通过且 bundle 体积可追踪 | `web/frontend/vite.config.ts` | `test_architecture_contracts.py` | — | `npm run build` 通过；ECharts、Vue ECharts adapter、Three、ELK、Vue/Pinia/Router 和轻量 vendor 分包可追踪，当前无 Vite chunk warning | OK | — |
| 5.14 | System monitor (CPU/MEM/DISK) | `web/api/routes/system.py` | `test_web_system_contracts.py` | `/system?tab=monitor` → `ActivityMonitor.vue` | 资源面板 + LLM provider 用量 + Top 进程 + API Health/Cron Jobs | OK | — |
| 5.15 | Monitor/Settings/Config Center 职责边界清晰 | `ActivityMonitor.vue` + `Settings.vue` + `ConfigCenter.vue` | `test_web_system_contracts.py` | `/system` tabs | Monitor 只读运行观测；Settings 管基础设置和审计；Config Center 按一级域 + 页面内二级分组编辑可调参数 | OK | — |
| 5.16 | 行业雷达 Web 页面 | `Sectors.vue` + `web/api/routes/sectors.py`, `web/api/services/sectors.py`, `data/market/sector_pipeline/` | `test_sector_pipeline.py`, `test_api_services.py`, `test_web_system_contracts.py` | `/research?tab=sectors` + `GET /api/sectors/*` | 行业资金方块矩阵主视图 + 行业面积按资金量映射 + 资金/动量/信号热力切换 + 申万行业排名表 + 行业级信号分布；不展示行业内具体股票；组合敞口归属组合执行页 | OK | — |
| 5.17 | Strategy Lab 目录化 UI + evidence artifact 下钻 | `Strategies.vue`, `StrategyLab.vue`, `StrategyEvidence.vue`, `web/frontend/src/api/index.ts` | `test_web_system_contracts.py:test_strategy_lab_exposes_catalog_and_candidate_language`, `test_strategy_evaluation.py` | `/strategy-lab?tab=strategies`, `/strategy-lab?tab=evidence`, `GET /api/strategies/evidence` | 首屏展示策略目录、生命周期筛选、候选策略、生产隔离横幅；证据面板展示 missing/available/blocked/promotion_ready 状态和 artifact 明细 | OK | — |
| 5.18 | CLI Control Plane | `astrolabe_cli/` | `test_cli_*.py` | `astroq health`, `astroq strategy catalog`, `astroq data status`, `astroq strategy evidence`, `astroq pipeline`, `astroq assets overview`, `astroq architecture ast`, `astroq execution dry-run`, `astroq lifecycle check` | Agent 可通过 JSON 输出判断下一步动作 | OK | — |
| 5.19 | Pipeline 关键参数透明度页面 | `web/api/routes/pipeline.py`, `web/api/services/pipeline.py`, `web/frontend/src/views/Pipeline.vue` | `test_pipeline_route_contracts.py`, `test_documentation_contracts.py` | `/pipeline` + `GET /api/pipeline` + `GET /api/pipeline/{key}` | Market Regime、Data Quality、Strategy Evidence、Portfolio Execution 四条流程图；节点可点击查看输入/输出 | OK | — |

## 6. 多资产架构 (Multi-Asset)

| # | PRD/Spec 条目 | 代码文件 | 测试 | API / Web | 手工验收 | 状态 | 缺口 |
|---|--------------|---------|------|-----------|---------|------|------|
| 6.1 | AssetAdapter ABC 统一接口 | `data/market/assets/base.py` | `test_asset_contracts.py` (27 tests) | — | `StockAsset` / `ETFAsset` 实现所有抽象方法 | OK | — |
| 6.2 | Stock 资产 (AKShare + Tushare) | `data/market/assets/stock.py` | — | `GET /api/stocks` → `/research?tab=stocks` | 全量 A 股股票池可查询 | OK | — |
| 6.3 | ETF 资产 (AKShare `fund_etf_hist_em`) | `data/market/assets/etf.py` | `test_asset_contracts.py::TestETFAssetContracts` | — | fetch_daily 返回标准 OHLCV, data_source="real" | OK | — |
| 6.4 | Bond/Futures/Crypto 适配器 | `data/market/assets/{bond,futures,crypto}.py` | `test_asset_contracts.py` (5 classes) | — | Bond=proxy, Futures=real, Crypto=placeholder | OK | — |
| 6.5 | AssetAllocator regime 动态权重 | `broker/allocator.py` | `test_broker_risk_persistence_allocator.py:test_asset_allocator_normalizes_regime_enum_and_unknown` | — | bull: stock 60%, bear: stock 15%, sideways: 35%；allocate 对启用资产归一化 | OK | — |
| 6.6 | 资产开关控制 (enabled: true/false) | `config/settings.yaml` assets.*.enabled, `data/market/assets/overview.py` | `test_multi_asset_tournament.py::TestAssetOverviewContracts` | `GET /api/assets/overview` → `/datahub?tab=assets` | 关闭资产在 CLI/API/Web 中显示 disabled，不进入 allocation | OK | — |
| 6.7 | 多资产回测对比 | `scripts/multi_asset_tournament.py` | `test_multi_asset_tournament.py` | — | stock-only vs ETF-only vs multi 三组对比，proxy fallback 保留 `data_source` | OK | — |
| 6.8 | 差异化费率 (A股/ETF/债券) | `broker/exchange.py` | — | — | A股卖出印花税 0.05% vs ETF 免印花税 | OK | — |
| 6.9 | 行业/板块数据维度 | `data/market/sectors.py` + `scripts/build_sector_snapshots.py` | — | `GET /api/sectors/*` | 申万行业指数 + 行业映射 + 信号聚合 + 敞口 | OK | — |
| 6.10 | 行业 Web 雷达页面 | `web/frontend/src/views/Sectors.vue` + `data/market/sectors.py` | `test_sector_pipeline.py`, `test_web_system_contracts.py` | `/research?tab=sectors` | 申万行业资金方块矩阵 + 按资金量映射面积 + 排名表 + 行业级信号分布，不显示行业内个股 | OK | — |

## 7. Agent Company OS

| # | PRD/Spec 条目 | 代码文件 | 测试 | API / Web | 手工验收 | 状态 | 缺口 |
|---|--------------|---------|------|-----------|---------|------|------|
| 7.1 | Agent Company OS 路线图和行为契约 | `docs/project/agent-company/00-master-roadmap.md`, `docs/specs/07-agent-company-os.md` | `astroq docs check --json` | — | 已完成阶段计划不留工作树；当前行为由 spec、验收矩阵、代码和测试追踪；未完成 live 验收计划单独保留 | OK | — |
| 7.2 | Agent Runtime 本地会话/消息/行动/运行 ledger | `agent_os/runtime.py`, `agent_os/ledger.py`, `agent_os/schemas.py` | `test_agent_os_contracts.py` | `GET/POST/PATCH /api/agent/sessions`, `GET /api/agent/sessions/{id}/stream`, `GET /api/agent/model-runtime`, `POST /api/agent/sessions/{id}/autonomy-step`, `POST /api/agent/sessions/{id}/autonomy-run`, `GET /api/agent/programs`, `POST /api/agent/sessions/{id}/programs`, `GET /api/agent/programs/{id}`, `POST /api/agent/programs/{id}/run`, `POST /api/agent/sessions/{id}/messages`, `POST /api/agent/plans`, `GET /api/agent/actions`, `POST /api/agent/actions/{id}/run`, `GET /api/agent/runs/{id}`, `GET /api/agent/runs/{id}/stream`, `GET/POST/PATCH /api/agent/work-orders`, `GET/POST /api/agent/memory(/export/prune/clear)`, `POST /api/agent/paper/proposals`, `POST /api/agent/paper/actions/{id}/submit`, `POST /api/agent/paper/actions/{id}/cancel`, `GET /api/agent/live/readiness`, `GET /api/agent/live/environment`, `POST /api/agent/live/smoke`, `POST /api/agent/live/proposals`, `POST /api/agent/live/actions/{id}/submit`, `astroq agent sessions/session create/session show/session update/autonomy step/autonomy run/programs/program create/program show/program run/message/plan/actions/action show/run/work-orders/work-order create/work-order update/paper propose/paper submit/paper cancel/live readiness/live environment/live smoke/live propose/live submit/memory export/memory prune/memory clear` | 创建 session/message/run 后可从 CLI/API 读取；session snapshot 和 run event snapshot 可通过 SSE 实时流读取且支持 once 模式；CEO message 会生成 deterministic desk response、web-route evidence、desk-scoped action、reasoning rows 和必要 handoff；CEO Office 可读取当前 `agent_planning` provider/model、推理程度、最大上下文和会话上下文使用估算；side-effect-free workflow plan preview 可在落 ledger 前展示 action、approval、handoff、planning mode、reasoning rows 和 work order 计划；bounded autonomy step 会创建普通 CEO message 和 desk response，只 dispatch 本轮新增的 `read_only/dry_run` fixed-registry actions，并跳过旧 pending action、审批写入、report artifact、paper/live order 和 code_change；bounded autonomy run 会在显式 `max_steps` 上限内重复同一安全边界，并在 `max_steps_reached`、`no_runnable_actions` 或 `step_not_ready` 时停机；autonomy program 可把开放式 CEO goal 持久化为多阶段计划，安全项成为 fixed-registry `read_only/dry_run` phases，未知/写入/代码/交易项成为 blocker、审批项或 Engineering work order，program dry-run 不写 action/run ledger；API/CLI 可显式提交 `semantic_draft` / `--semantic-draft-file`，外部 planner 草案会被当作不可信输入过滤到 `read_only/dry_run` fixed-registry 动作、标记人工复核，并且 plan preview 不写 ledger；CEO follow-up 可读取当前 session backlog，把待审批 action、open handoff 和 open work order 转成 `adaptive_session` 安全 re-check 计划；当 follow-up 同时要求基于当前证据排优先级时，可生成 `adaptive_artifact` 计划，融合 session backlog 与 artifact root causes 并去重安全动作；open-ended 公司运营请求可生成 diagnostic-only `open_ended_adaptive` 计划，跨 Data/Research/Portfolio/Risk/Execution/Engineering 只提出 read-only/dry-run 动作；session title/status/tags 可更新；action list 支持按 session/status/desk/risk 过滤；固定 registry 命令可 dispatch 并记录 succeeded/failed/blocked run；dispatch 对真实子进程会按 stdout/stderr 行写 run event chunk，并保留 queued/running/terminal run event timeline，`GET /api/agent/runs/{id}`、`GET /api/agent/runs/{id}/stream` 和 action detail API 可读取；Engineering Desk 可创建/list/update auditable work orders，包含 evidence、impact、affected files、suggested verification、resolution 和 terminal timestamp，并进入 memory snapshot；已批准 fixed-registry 模板命令可按参数 schema 安全绑定；PaperBroker preview 通过后可生成 approval-required 订单行动卡和 preview evidence；CEO Office 在消息内 action card 显示 paper preview/risk、提交 approved paper action、取消 queued/active paper action，并从运行记录链接 paper reconciliation evidence；approved paper submit 会二次 preview/risk gate、阻断 stale approval、写 run/reconciliation evidence 并持久化默认 broker state；live readiness/environment 只读验证本机 SDK、账号、userdata、gateway 和终端查询能力，不下单、不回退 PaperBroker；live smoke 不下单、不回退 PaperBroker，live ready 时只读调用 reconciliation probe 并写 smoke evidence；live proposal/approved submit contract 会二次 live preview/risk gate、只通过 live adapter 提交、写 live reconciliation evidence，并在 live submit/scheduled reconciliation 前附加 preview-derived project ledger snapshot，默认 adapter fail closed；transparent memory 可 inspect/export 到 `var/artifacts/agent/memory/`，并支持 archived session prune 与显式确认 clear | OK | — |
| 7.2b | Agent prompt context compression | `agent_os/context.py`, `agent_os/runtime.py`, `agent_os/semantic_planner.py` | `test_agent_os_contracts.py`, `test_ceo_office_frontend_contracts.py` | `GET /api/agent/sessions/{id}/context`, `POST /api/agent/sessions/{id}/context/compact`, `GET /api/agent/model-runtime`, `astroq agent context status/compact` | 长会话接近 provider context window 时，runtime 会在 provider planning 前自动构建 context pack；最近消息保留原文，较早消息压缩为确定性摘要并可由配置 provider 增强，evidence/action refs 保留；原始 message ledger 不删除不改写；GET context 不写 artifact，compact 或 provider 前压缩写入 `var/artifacts/agent/context/`；压缩后仍超过硬阈值时返回 blocker 且不调用 provider；CEO Office 只在最外层 Web 底部状态栏里用 CTX 电池格颜色表达上下文状态，不新增压缩按钮、状态文案或调试面板 | OK | — |
| 7.2a | Provider semantic planner 显式接入 | `agent_os/semantic_planner.py`, `agent_os/workflows.py`, `data/llm/usage.py` | `test_agent_os_contracts.py` | `POST /api/agent/plans`, `POST /api/agent/sessions/{id}/messages`, `POST /api/agent/sessions/{id}/autonomy-step`, `POST /api/agent/sessions/{id}/autonomy-run`, `astroq agent plan/message/autonomy step/autonomy run --provider-semantic` | `planner_mode=provider_semantic` 和 `--provider-semantic` 只在显式开启时调用配置的 env-only LLM provider；API/CLI 可传 request-scoped provider/model override，空值使用 `agent_planning` 默认配置；CEO Office 不暴露 Provider Planner 控件；provider 未配置、`enabled=false`、缺配置的 `api_key_env`、缺 base_url/model 或 unsupported protocol 时返回结构化 blocker，不调用 transport、不写 agent ledger、不回退成假成功；发送给 provider 的本地 artifact/session context 会递归脱敏 secret-like key；provider 返回内容只作为不可信草案，经 fixed-registry、desk scope、tool 参数 pattern 和 `read_only/dry_run` 风险过滤后才能进入 preview、action card、bounded autonomy step 或 bounded autonomy run；缺 desk 的语义行动只有在 safe tool 恰好属于单一 desk scope 时才可确定性归属，多 desk scope 仍拒绝；未声明参数不会透传，缺失或非法 templated 参数会拒绝该 action；非数值 confidence、单对象 actions/reasoning 或字符串 blocker 等 metadata 异常会规范化成可审查的 blocked plan，不抛 API/CLI/runtime 异常；reasoning 不泄露 API key；provider 返回 usage 时写入通用 LLM usage ledger 的 `agent_planning` source；缺 pricing 时标记 unpriced，不借用其他 provider 价格；preview 不写 agent ledger/evidence，提交 CEO message 或 bounded autonomy step/run 时会登记脱敏 `Semantic planner audit` artifact evidence；bounded autonomy 只 dispatch 当前 step 新增且过滤通过的 `read_only/dry_run` fixed-registry actions | OK | — |
| 7.2c | RouterAgent 混合分诊与部门事实回答 | `agent_os/router.py`, `agent_os/runtime.py`, `agent_os/workflows.py`, `config/settings.yaml` | `test_agent_os_contracts.py` | `POST /api/agent/sessions/{id}/messages`, `llm.use_cases.agent_routing`, `llm.providers.mimo_router` | Web CEO message 不传 `desk` 时先走本地单部门规则；无命中或跨部门冲突才调用 `agent_routing` OpenAI-compatible provider；provider 只返回结构化分诊 JSON，未知 desk、缺 secret、超时或非法 JSON fail closed 回退本地路由；路由 reasoning 记录 `router_source/confidence/intent/supporting_desks`；Research 对策略数量/策略目录事实问题读取 `research.strategy_catalog.catalog_items()` 返回真实总数、layer/lifecycle 分布，不运行回测、不写 artifact、不创建交易/修复行动；Data/Risk/Engineering 状态事实问答只读本地 artifact，缺 artifact 明确返回缺口和推荐命令 | OK | — |
| 7.3 | ApprovalPolicy 审批系统 | `agent_os/approval.py`, `agent_os/runtime.py` | `test_agent_os_contracts.py` | `GET /api/agent/policies`, `POST /api/agent/actions/{id}/approve`, `POST /api/agent/actions/{id}/reject`, `POST /api/agent/actions/{id}/cancel`, `POST /api/agent/actions/expire`, `astroq agent policies/approve/reject/cancel/expire` | 每个 risk level 都有显式 policy row，可通过 CLI/API 查看默认决策、要求角色、过期窗口和原因；CEO Office 只通过 action 状态暴露需要审批的事项，不常驻展示 policy 表；write/backfill/report-artifact/backtest/paper/live/code risk level 默认进入 approval-required 或 work-order-required 状态；未审批 action dispatch 只记录 blocked run，不执行命令；proposed/approval-required/approved action 可在终态前取消；queued action 有 `expires_at`，过期后不能审批或执行；已成功/失败 action 不会被改写 | OK | — |
| 7.4 | EvidenceRef 证据解析 | `agent_os/evidence.py`, `agent_os/runtime.py` | `test_agent_os_contracts.py` | `GET /api/agent/evidence/{evidence_id}`, `astroq agent evidence` | missing/fresh artifact evidence 返回结构化状态和 sha256 hash；file-backed evidence 会复制到 `var/artifacts/agent/evidence/`，源文件变化返回 `source_changed`，源文件丢失但快照存在返回 `source_missing`；`web_route` evidence 只为安全本地 route 返回 navigation 元数据；仓库内 `file`/`code` evidence 返回 CodeGraph 文件深链，`code` URI 支持 `path:line`；`api_endpoint` evidence 只为本地 `/api/...` GET 返回 navigation；`cli_command` evidence 只为 inert `astroq` / `.venv/bin/astroq` 命令返回 argv 元数据，拒绝 shell syntax 和外部路径；仓库外已存在文件只允许 snapshot/hash，不生成本地导航 | OK | — |
| 7.5 | CEO Office 默认首页 | `web/frontend/src/views/CEOOffice.vue`, `web/frontend/src/router/index.ts`, `web/frontend/src/api/modules/agent.ts` | `test_ceo_office_frontend_contracts.py`, `test_frontend_i18n_contracts.py`, browser smoke | `/` CEO Office, `/market` 市场总览 | 默认首页显示 CEO 对话、确定性自动部门分诊、session snapshot 与 run event snapshot 的 auth-aware SSE 实时流状态、deterministic desk response、消息内 evidence/action card、需要审批时的内联审批/拒绝/取消/纸面订单提交决策、可展开 run history/run event timeline/run artifact evidence links，以及安全 web-route evidence 深链；消息列表独立滚动，输入和发送固定在 CEO 页面底部，当前 provider/model/推理程度/CTX telemetry 收敛到最外层 Web 底部状态栏；市场总览迁移到 `/market`；CEO Office 不常驻展示目标部门选择器、行动队列、右侧 action/evidence detail、desk drill-down、审批策略、handoff 面板、Engineering work orders、report cards、report rhythm、live readiness/kill-switch/monitor 控件，也不暴露 workflow preview、semantic draft、Provider Planner、自主步骤/循环或 autonomy program 控件，开放式计划、显式部门指定和语义辅助仍通过 CLI/API 内部运行机制处理 | OK | — |
| 7.6 | Desk agents 权责和工具权限 | `agent_os/desks.py`, `agent_os/runtime.py`, `agent_os/ledger.py`, `agent_os/tools.py`, `agent_os/workflows.py` | `test_agent_os_contracts.py` | `GET /api/agent/desks`, `GET /api/agent/handoffs`, `POST /api/agent/handoffs/{id}/resolve`, `GET/POST/PATCH /api/agent/work-orders`, `astroq agent desks/handoffs/handoff resolve/work-orders/work-order create/work-order update` | Data/Research/Portfolio/Risk/Execution/Engineering/Reporting desks 有 mandate、allowed_tools、forbidden_actions、evidence_required、handoff_targets；Research 的 technical/sentiment/fundamental/factor/ML 是子能力，不拆成一级 desk；所有 desk-declared fixed tools 均存在于 `AgentToolRegistry` 并带 desk scope；固定 registry 工具在 propose 和 dispatch 阶段均执行 desk scope 校验；CEO message 可触发 deterministic desk workflow，并按意图路由到数据源 registry diff、Data Desk 补数据 dry-run + approval-required write action、Research 策略阻断诊断（strategy competition + DataHub health + lifecycle gates）、Portfolio 组合决策证据审查（strategy evidence + lifecycle + execution readiness）、Execution MiniQMT/QMT live readiness 只读检查、Reporting daily report artifact approval action、Engineering 代码/bug 请求工单化（AST + test design diagnostics + auditable work order）、策略竞争、回测 dry-run、测试设计和 docs check 等具体工具；Data source/repair、Research strategy-blocker、Portfolio decision review、Risk lifecycle、Execution readiness 和 Engineering diagnostics 会把当前 artifact 摘要写入 desk answer 和 artifact_context reasoning；Reporting daily brief 意图会生成 Data/Research/Risk 三个 desk-scoped read-only actions 和独立 evidence refs；Reporting 显式生成 daily CEO report artifact 请求会创建 `write_artifact` 审批动作并绑定当前 session；Reporting portfolio review 意图会生成 Research/Portfolio/Risk/Execution 四个 desk-scoped safe actions，覆盖 strategy competition、portfolio decision evidence、lifecycle readiness 和 execution dry-run；Reporting mixed CEO review 请求可生成确定性 `dynamic_multi_intent` 计划，组合 Data source diff、Research strategy competition、Portfolio decision review、Risk lifecycle、Execution dry-run 和 Engineering test/AST diagnostics，同时保留固定 tool id、去重和 handoff；Reporting broad priority 请求会读取固定本地 artifact context，并生成 `artifact_aware` 计划，把 data source gap、lifecycle blocker、strategy evidence blocked、AST/test-design risk 映射到 owner desks 的安全动作；Reporting daily brief、portfolio review、`dynamic_multi_intent`、`artifact_aware`、`adaptive_artifact` 和 `open_ended_adaptive` 的 desk answer 与 artifact_context reasoning row 会展示 bounded evidence summary，例如 blocker 维度/原因、未映射 source capability 数、阻断策略比例和 AST/test-design 风险数量；Reporting follow-up 请求可读取 session backlog，生成 `adaptive_session` 计划，将待审批 data repair 复核为 dry-run，将 open handoff/work order 映射到 owner desks 的安全证据动作；当 follow-up 同时要求当前证据/优先级判断时，会生成 `adaptive_artifact` 计划，把 session backlog 与 artifact root causes 合并为去重后的安全动作，并输出 context_fusion reasoning row；Reporting open-ended 公司运营请求会生成 `open_ended_adaptive` 诊断计划，固定拆到 Data status、Research catalog、Portfolio decision review、Risk lifecycle、Execution dry-run、Engineering AST/test design，并输出 open_goal_decomposition reasoning row；显式 semantic planner draft 可从 runtime/API/CLI 输入，经服务端过滤后只保留 desk-scope 匹配的 `read_only/dry_run` fixed-registry 工具，并输出 accepted/rejected reasoning 和人工复核 blocker；workflow preview 和 desk response 会输出 intent/tool/safety/evidence/session backlog/session/artifact context/context fusion/open goal decomposition/semantic planner reasoning rows；Engineering Desk 不直接改仓库，只创建带证据、影响、影响文件和建议验证命令的工程工单，并可将工单状态推进到 `in_progress/resolved/canceled`；结构化 desk response、open/resolved handoff ledger、engineering work-order ledger 和终态审计时间可验证 | OK | — |
| 7.7 | MiniQMT/QMT live execution adapter | `broker/live/qmt.py`, `broker/live/xtquant_gateway.py`, `agent_os/runtime.py`, `web/frontend/src/views/CEOOffice.vue` | `test_agent_os_contracts.py`, `test_xtquant_gateway.py`, `test_ceo_office_frontend_contracts.py` | `GET /api/agent/live/readiness`, `GET /api/agent/live/environment`, `POST /api/agent/live/smoke`, `POST /api/agent/live/preview`, `POST /api/agent/live/proposals`, `POST /api/agent/live/actions/{id}/submit`, `POST /api/agent/live/reconciliation`, `POST /api/agent/live/monitor`, `GET/POST /api/agent/live/kill-switch(/activate,/deactivate)`, `astroq agent live readiness/environment/smoke/preview/propose/submit/reconcile/monitor/kill-switch`, CEO Office action/evidence references | live 默认关闭；缺 SDK/权限/account/kill switch 返回 blocked readiness，不 import crash；live environment 只读验证 SDK、登录/account、权限、userdata path、gateway load 和 gateway terminal query，不调用下单且不回退 PaperBroker；xtquant gateway 的 environment validation 只调用 connect/subscribe/query asset/positions/orders/trades，不调用 `order_stock()`；xtquant gateway 撤单只调用 SDK cancel 方法，不调用 `order_stock()`；live smoke 不提交订单、不回退 PaperBroker，blocked readiness 停在 readiness，live-ready broker 只收到 read-only reconciliation probe 并写 `var/artifacts/agent/live_smoke/` evidence；订单预览统一返回 `approval_required=true`、`submitted=false`、`paper_fallback=false`，并基于 readiness、evidence、现金、单票集中度、总敞口、日订单数、可交易性、数据新鲜度、券商账户一致性、回撤、VaR、CVaR、行业集中度和日内涨跌停状态给出 risk gate 阻断；live proposal 只在 preview 通过后生成 approval-required action；approved submit 会重新 preview/risk gate、只调用 live adapter、写 `live.live_order.submit` run 和 live reconciliation evidence，并在 adapter reconcile 前附加 project ledger snapshot；scheduled reconciliation 扫描已提交 live_order evidence，复用 project ledger snapshot 调用 live adapter reconcile 并写 scheduled artifact；live monitor tick 汇总 readiness、kill switch 和 scheduled reconciliation，写 `var/artifacts/agent/live_monitor/` evidence，适合 cron 调用；CEO Office 不常驻显示 live monitor 控件，异常通过消息/action/evidence 或全局状态入口暴露；本地 kill switch 可激活/解除，激活时取消 queued live_order actions，扫描 submitted live evidence，对支持 `cancel_order()` 的 live adapter 请求 broker-side 撤单，不支持或失败写 blocked evidence，并在触碰 live adapter 前阻断 preview/propose/submit；默认 MiniQMT adapter 返回 `live_submission_not_integrated` / `not_integrated`，不伪造实盘订单或对账；显式注入 SDK gateway 或配置 `execution.live.sdk_gateway_factory` 后可走可审计 submit/reconcile/cancel，并对原始响应做脱敏和哈希；xtquant gateway 会比较 project snapshot 与 broker cash/positions/orders，快照缺项或缺对账能力返回 `needs_review` 而不是 fake matched；factory 加载失败会阻断 readiness | PARTIAL | 真实 MiniQMT/QMT 终端撤单语义和定时对账仍需人工演练 |
| 7.8 | CEO reports and operating rhythm | `agent_os/reports.py`, `agent_os/runtime.py`, `agent_os/notifications.py`, `web/frontend/src/views/CEOOffice.vue` | `test_agent_os_contracts.py`, `test_ceo_office_frontend_contracts.py` | `GET/POST /api/agent/reports`, `POST /api/agent/reports/{id}/notify`, `POST /api/agent/reports/rhythm`, `POST /api/agent/reports/rhythm/scheduled`, `astroq agent reports/report/notify report/rhythm (--session or --all-active)`, CEO Office message/evidence references | Daily CEO brief、weekly review、audit pack、data quality、risk、execution reconciliation、engineering digest、release audit 写入 `var/artifacts/agent/reports/` JSON/Markdown，引用 session/action/run/handoff evidence，缺失 evidence 明确列出，并登记为可解析 `report` evidence；报告包含固定白名单 `artifact_context`，聚合 lifecycle、data source、strategy competition、AST、test design 和 CodeGraph readiness，并在 `artifact_readiness` / `artifact_findings` sections 中展示阻断、问题和风险线索；报告还包含确定性 `semantic_synthesis` section，把本地 artifact 摘要与 findings 汇总为 root causes、impacts 和 next actions，不调用 LLM；报告包含确定性 `domain_scorecard` section，把 root causes 映射到 Data/Research/Portfolio/Risk/Execution/Engineering/Reporting desk 状态和推荐 `astroq` 命令；报告包含确定性 `source_narratives` section，把每个固定 artifact 源转成 desk-owned status、headline、evidence summary 和 recommended command；报告包含确定性 `artifact_timelines` section，把当前固定 artifact 的 status/summary/finding count 与最近一次历史 report 对比；报告包含确定性 `trend_synthesis` section，把当前 root causes 和历史本地 report artifacts 对比，标出重复 blocker；报告包含确定性 `causal_chain_synthesis` section，把 data source gap、lifecycle blocker、strategy evidence blocked、engineering/test-design risk 组合成 owner-specific chains 和 next actions，并把历史报告中重复出现的 causal chain 标记为 recurring、升级到 standing owner review；报告可通过 CLI/API 生成并登记为 evidence；CEO Office 不常驻显示 report cards、模板选择器、rhythm 或通知控件，报告结论通过消息内 action/evidence card 或系统页深链进入 CEO 视野；`--all-active` / scheduled API 会扫描 active sessions 并写 scheduled rhythm audit artifact，适合 cron 调用；报告通知只读系统环境变量，支持 Telegram/WeChat/Feishu 的发送或 dry-run，并写入 `var/artifacts/agent/reports/notifications/` 审计 artifact | OK | — |

## 汇总

| 能力域 | 总条目 | 功能可验收 | 规划中 | 质量债条目 | 待补自动化测试 |
|--------|-------|------------|--------|------------|----------------|
| 数据管道 | 13 | 13 | 0 | 0 | 0 |
| 信号系统 | 17 | 17 | 0 | 0 | 0 |
| 回测引擎 | 10 | 10 | 0 | 0 | 0 |
| 执行层 | 9 | 9 | 0 | 0 | 0 |
| Web 平台 | 19 | 19 | 0 | 0 | 0 |
| 多资产架构 | 10 | 10 | 0 | 0 | 0 |
| Agent Company OS | 11 | 10 | 0 | 1 | 0 |
| **合计** | **89** | **88** | **0** | **1** | **0** |

> **说明：** `功能可验收` 表示该能力有完整可用代码路径。`规划中` 表示已有产品/行为契约但还未实现，不计入当前功能完成数。`质量债条目` 统计"缺口"列非 `—` 的行，包含已实现能力的质量债和规划能力的未实现缺口。当前剩余缺口只剩 Agent Company OS 的真实 MiniQMT/QMT 终端撤单语义和定时对账人工演练；12 策略 OOS/IC/ICIR 竞赛和 measured baseline evidence 已接入，但大多数策略因收益、换手或 alpha 门槛未达标仍保持 candidate/paper recommendation。开放式 CEO 目标现在通过 autonomy program 持久化为多阶段计划；执行边界仍故意限定为 fixed-registry 的 `read_only` / `dry_run`，写入、代码、paper/live 交易和未知工具必须进入审批、工单或 blocker。

**维护说明:**

- 本矩阵只保留当前能力与验收链路，具体完成时间从 git 历史追溯。
- 新能力进入矩阵前必须能映射到代码、测试或手工验收方式。
- 未来重点仍是扩大真实浏览器 smoke/e2e、视觉回归、数据质量门禁和研究实验治理。
